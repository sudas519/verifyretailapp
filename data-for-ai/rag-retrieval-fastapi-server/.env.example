# =============================================================================
# RAG Retrieval FastAPI Server - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# This server provides REST API endpoints for semantic and keyword search

# =============================================================================
# Server Configuration
# =============================================================================
# Host and port for the FastAPI server
HOST=0.0.0.0
PORT=8080

# Server identification
SERVER_NAME=rag-retrieval-api
SERVER_VERSION=1.0.0

# Server description (optional)
SERVER_DESCRIPTION=FastAPI RAG Retrieval Server for semantic and keyword search

# Environment: development, staging, or production
ENVIRONMENT=development

# Optional: Comma-separated list of allowed origins for CORS (* for all)
ALLOWED_ORIGINS=*

# Optional: Bearer token for API authentication (leave empty to disable)
APP_BEARER_TOKEN=

# =============================================================================
# IBM Watsonx Embeddings (REQUIRED)
# =============================================================================
# Used to generate embeddings for semantic search queries
# Get these values from your IBM Watsonx AI project

# Watsonx API endpoint URL
WATSONX_URL=https://us-south.ml.cloud.ibm.com

# Watsonx API key (IAM API key with access to Watsonx)
WATSONX_API_KEY=your_watsonx_api_key_here

# Watsonx project ID (from your Watsonx project)
WATSONX_PROJECT_ID=your_watsonx_project_id_here

# Embedding model ID (must match the model used during ingestion)
# Recommended: intfloat/multilingual-e5-large
EMBEDDING_MODEL_ID=intfloat/multilingual-e5-large

# =============================================================================
# Vector Database Selection (REQUIRED)
# =============================================================================
# Choose which vector database to use: milvus
# Must match the database used during ingestion
VECTOR_DB_TYPE=milvus

# =============================================================================
# Milvus Configuration (REQUIRED)
# =============================================================================
# Milvus vector database connection details

# Milvus host (hostname or IP address)
MILVUS_HOST=localhost

# Milvus port (default: 19530)
MILVUS_PORT=19530

# Milvus username for authentication
MILVUS_USER=root

# Milvus password for authentication
MILVUS_PASSWORD=Milvus

# Use secure connection (TLS) for Milvus (true/false)
MILVUS_SECURE=false

# Collection name to search (must match the collection used during ingestion)
MILVUS_COLLECTION=product_insights

# =============================================================================
# Milvus Field Configuration (Optional)
# =============================================================================
# Field names in the Milvus collection schema
# Use defaults unless your schema differs from the ingestion server

# Dense vector field name (default: "vector")
MILVUS_DENSE_FIELD=vector

# Text field name (default: "text")
MILVUS_TEXT_FIELD=text